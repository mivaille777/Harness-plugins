# Harness Plugins 开发任务与 Codex 执行手册

> 文档状态：持续维护的开发执行手册。初始编写日期：2026-09-05；执行核查更新：2026-09-09。目标仓库：[mivaille777/Harness-plugins](https://github.com/mivaille777/Harness-plugins)。本文的目标、接口扩展、指标和新增命令均为开发要求；当前能力和测试证据以本仓库的实现及 `docs/evidence/` 为准。

## 摘要

本计划指导 Codex 把 `dsh-selection-companion` 从浏览器选区采集桥接器，建设为保持阅读上下文的 Windows AI 助手。最终用户能够选中文字、在原位获得解释、核查来源、继续追问，并返回原文；系统能够准确关联选区、上下文、请求和会话，提供可恢复的失败处理。语言转换不属于本产品范围；产品重点是增强用户对所选材料的感知和理解。

本文应版本化保存于目标仓库根目录。后续开发必须先确认目标仓库、分支和提交，不得在其他 DeepSeek Harness 工作区误改插件项目。没有对应证据的功能仍保持未验收状态；真实模型、审批、安装和人工人因效果不能用模拟测试代替。

## 目录

- [1. 使用方式与执行约束](#1-使用方式与执行约束)
- [2. 产品目标、范围与人因要求](#2-产品目标范围与人因要求)
- [3. 源码基线与关键缺口](#3-源码基线与关键缺口)
- [4. 架构、状态与数据责任](#4-架构状态与数据责任)
- [5. 任务依赖与里程碑](#5-任务依赖与里程碑)
- [6. 分阶段开发任务](#6-分阶段开发任务)
- [7. 测试命令与证据规则](#7-测试命令与证据规则)
- [8. ChatGPT 与 Codex 并行工作流程](#8-chatgpt-与-codex-并行工作流程)
- [9. PR、验收与任务交接模板](#9-pr验收与任务交接模板)
- [10. 最终完成定义](#10-最终完成定义)
- [11. 依据与资料](#11-依据与资料)
- [12. 文档维护说明](#12-文档维护说明)

## 1. 使用方式与执行约束

### 1.1 如何下达任务

每次将本文件提供给 Codex，并复制对应任务中的完整提示词。提示词要求 Codex 读取本文件的公共约束和该任务正文，因此不能脱离本文件单独执行。默认一次执行一个任务编号；具备明确依赖和文件所有权后，可以按第 8 节并行。

开发者先完成 T00，再完成 T01，随后按依赖关系推进。任务中的建议目录可在阅读真实源码后调整；行为要求、验收标准和责任归属不能在没有记录的情况下被削弱。发现基线变化时先核对差异，不得回退用户代码以迎合本计划。

### 1.2 仓库与权限

- 目标是 `mivaille777/Harness-plugins`；DeepSeek Harness 主仓库是宿主依赖，不是默认修改范围。
- 先读取目标检出中的 `AGENTS.md` 及目录级规则。用户提供的 Harness 规则用于宿主集成约束；不能假定插件仓库已有宿主的全部脚本、目录或文档检查器。
- 修改宿主 `packages/` 前必须读取该宿主版本的 `docs/architecture.md`；生命周期、并发、进程与退出工作须读取适用的防御模式说明。需要改宿主时单独提出最小变更并建立独立 PR。
- 实现、测试、文档和本地可逆构建属于开发任务范围。向远端推送、创建外部评论、合并、发布、签名、上传遥测或更改系统级配置必须遵循当次会话授权；仅持有本计划不代表已获得这些授权。
- 不覆盖已有未提交改动，不在共享检出切换他人分支，不使用强制重置清理现场，不自动安装开机启动或覆盖用户现有 profile。
- 本文不授予无限运行或无限模型消费预算；真实模型测试遵循使用者已有的密钥与成本授权。

### 1.3 所有任务的公共完成要求

1. 列出修改范围、前置任务、行为变化和受影响接口；先读代码再实施。
2. 对进程间、文件、模型输入和配置进行必要验证；不为静态类型已保证的同进程数据增加无意义防御代码。
3. 通过 Cordis 服务、插件和事件接入 Harness。注册必须可撤销；不另建 LLM 客户端、会话文件或独立对话数据库。
4. 新增模型可见输入必须能够从 Harness 会话日志重建。需要扩展宿主事件或 SDK 时，遵循该版本的 TypeScript/Python 投影规则。
5. 配置项须有所有者、默认值、范围、应用时机和错误行为。协议常量和安全不变量固定；部署相关调节项不得仅藏在常量或测试钩子中。
6. 使用类型化本地化字典管理 UI 文案、错误摘要、占位符和无障碍名称；原始技术错误仅在诊断详情显示。
7. 非平凡实现同步更新 README、必要 JSDoc 和决策记录。若目标仓库尚无 Agent Note 机制，在 T00 建立最小记录约定，避免复制整个宿主文档体系。
8. 运行受影响的最小有效测试集合。发布候选运行完整聚合检查；不要为了每次提交重复整套无关测试。
9. 报告实际执行的命令、退出码、通过/失败/跳过数量、平台、提交 SHA 和未验证项。编译成功不等于功能成功，模拟测试通过不等于原生链路通过。
10. 测试未执行或前置能力缺失时保持任务未验收；不得通过忽略错误、空测试、默认更新全部快照来制造成功。

## 2. 产品目标、范围与人因要求

### 2.1 产品最终目标

用户在受支持应用中选择内容后，可以用一次明确动作请求帮助。系统保留选区来源及必要上下文，显示简短、可核查的解释，并支持上下文追问、停止生成、转入完整 Harness 会话及返回原文。用户能够理解系统正在处理什么、内容来自哪里、发送了哪些上下文，以及出现失败后如何恢复。语言转换不属于本产品能力，界面不得提供翻译入口或将其写入验收标准。

“感知增强”分为两部分：机器识别用户所指内容及其上下文；用户以更低操作成本获得更准确的理解。功能数量、动画数量、采集范围和模型调用频率都不是成功指标。

### 2.2 MVP 范围

| 纳入首个完整版本 | 明确不属于 MVP |
|---|---|
| Windows 10/11，Chrome/Edge 普通文本页面 | macOS/Linux 原生采集 |
| UIA 默认采集，不要求安装浏览器扩展 | 默认安装扩展或默认接管剪贴板 |
| 解释、上下文追问 | 自动替用户编辑第三方应用内容 |
| 选区、局部上下文及可获得段落 | 保证所有页面、扫描 PDF 和所有应用可读 |
| Lens、会话绑定、流式回答、取消与恢复 | 无用户请求的持续模型监视或自动总结 |
| 可查看引用、选择上下文范围、暂停采集 | 单独维护第二份对话历史 |
| 键盘、中文输入法、多屏与 DPI 支持 | 眼动、情绪、疲劳或生理状态推断 |

通用 UIA、Word COM、DOM 富上下文和更完整 PDF 支持属于 M3 扩展，不能阻塞浏览器 MVP。可选功能即使未完成，也必须在界面和支持矩阵中准确标记；不能显示可用入口后静默失败。

### 2.3 主要用户任务

| 场景 | 输入与动作 | 可观察的完成条件 |
|---|---|---|
| 理解术语 | 选中术语，点击“解释” | 回答结合本段语境，保留原文引用 |
| 理解复杂材料 | 选中术语或段落，点击“解释”并继续追问 | 回答结合选区语境，原文与来源仍可核对 |
| 追问论证 | 固定选区，输入具体问题 | 回答仍绑定相同选区，来源可展开 |
| 扩大依据 | 主动选择更多上下文 | 明确显示扩展结果、范围与截断 |
| 持续研究 | 从 Lens 转入完整会话 | 相同 sessionId 的同一段历史可见 |
| 恢复阅读 | 关闭 Lens | 原窗口和阅读位置保持，键盘可继续操作 |

### 2.4 人因设计要求

| 原则 | 必须实现的交互 | 对应验收 |
|---|---|---|
| 降低任务切换成本 | 轻量入口接近选区，正文回答不过度遮挡原文 | 测量窗口切换和恢复阅读时间 |
| 降低工作记忆负担 | 始终显示当前问题对应的选区摘要、来源、会话 | 用户能正确指出回答针对的材料 |
| 渐进展示 | 第一层仅显示解释、追问；高级项展开 | 首次用户无需教程即可完成主要任务 |
| 减少运动成本与误触 | 按钮有足够命中区域，避开拖选路径 | 多 DPI 与键盘路径无误触和不可达控件 |
| 保持控制感 | 暂停、关闭、停止、重试和切换上下文具有不同语义 | 用户动作与后台状态一致 |
| 降低模式错误 | 区分当前选区、已固定选区、生成中的请求 | 切换选区不会悄悄更换已提交内容 |
| 校准信任 | 原文证据与模型解释分开；标明缺失和截断 | 不显示未经校准的“答案可信度百分比” |
| 控制注意力 | 被动提示不抢焦点、不发声、不调用模型 | 阅读和普通复制操作不触发模型请求 |

数据保留策略必须明确：临时采集数据可以清除；已经提交并写入 Harness 会话的数据由宿主持久化规则管理。清除临时选区不能显示为“删除全部历史”，也不能隐式删除会话。

### 2.5 视觉设计方向

采用阅读工具风格：暖白与石墨灰双主题、一个强调色、清晰正文、克制边框和阴影。先定义颜色、字体、间距、圆角、阴影、动效和层级变量，再实现组件。正文建议 14–16 CSS px；按钮建议 32–40 CSS px 高，实际命中区域与 WCAG 适用要求共同验证。不要依赖透明背景、细灰字和大量渐变制造质感。

保留折叠入口、展开 Lens、完整会话三种层次。Pipe、Protocol、Ping、内部任务编号放入诊断页。长答案支持展开与稳定滚动；流式输出不反复抢滚动位置。状态同时使用文字或图形说明，不能仅依靠颜色。

WebView 内容以 WCAG 2.2 AA 为参考：普通文字对比度至少 4.5:1，适用的大字至少 3:1；焦点可见且不被遮挡；支持键盘、缩放和减少动画。24×24 CSS px 最小目标尺寸存在标准规定的例外，不能把按钮高度一个值当作完整合规证明。原生焦点、窗口和 Narrator 行为须另外测试。

## 3. 源码基线与关键缺口

### 3.1 审阅基线

| 路径 | 当前职责 | 本计划中的改进方向 |
|---|---|---|
| `src/index.ts` | 安装上下文与桥接服务 | 配置传递、宿主适配与可撤销贡献 |
| `src/context/` | 不可变快照、修订顺序、TTL、容量限制 | 清除语义、固定引用、明确范围 |
| `src/bridge/` | 编解码、协议、请求追踪、路由、服务端 | 生命周期、超时、会话接入和订阅 |
| `native/src-tauri/src/capture.rs` | UIA 事件、采集工作线程、去重 | 生命周期状态、有界队列、暂停与失效 |
| `native/src-tauri/src/providers/` | 默认浏览器 UIA Provider | 精确几何、能力与来源质量 |
| `native/src-tauri/src/bridge.rs` | Named Pipe 客户端与连接状态 | 超时、取消、异步事件分发 |
| `native/src/App.tsx` | 连接诊断 React 界面 | Lens、输入、回答和用户状态 |
| `browser-extension/` | 可选 DOM 与 Native Messaging 路径 | 按需上下文，不改变默认安装路径 |
| `tests/protocol/` | TS/Rust 共用 JSON 样例 | 双方正反例与事件一致性 |

表中的路径来自审阅提交，不保证后续版本保持不变。开发者须使用 `rg --files` 和包脚本核对实际布局。

### 3.2 已观察的行为与待验证风险

- 路由实现了握手、Ping、选区更新和当前选区读取；协议中存在会话、订阅等消息，不代表业务实现已经可用。
- `NoSelection` 分支未发布失效事件。需要新增行为验证，确认取消选区不会继续使用陈旧内容。
- 采集到发布之间存在无界队列；Rust IPC 交换没有显式超时。需要测试服务端无响应时的队列和取消行为。
- 提交选区会尝试连接；用户断开连接与后台自动恢复可能冲突，需要明确定义用户暂停状态。
- 窗口配置启动时可见、聚焦、置顶。需要验证是否影响选区和阅读，并设计非激活入口。
- 几何信息采用外围可访问元素矩形，不是精确文字范围；不能据此承诺贴近选区的位置效果。
- 元数据加权分数表示采集信息完整程度，不表示模型答案的正确概率。
- 现有原生 UI 测试以模拟桥接为主，浏览器 Provider 测试包含辅助函数检查；不能替代 UIA 实机测试。
- 审阅文件树未见 pnpm/Cargo 锁文件及 GitHub Actions 工作流；Tauri 安装打包未启用。T00/T10 需重新核对并完成各自职责。

## 4. 架构、状态与数据责任

### 4.1 目标数据流

```text
受支持应用中的用户选区
  → Native Provider：采集文字、来源、局部上下文、几何
  → CaptureRuntime：新鲜度、失效、去重、暂停、容量控制
  → 本地 Bridge：握手、请求关联、超时、能力协商
  → Harness SelectionContext：当前快照与有限临时缓存
  → 用户打开 Lens：固定 snapshotId + revision
  → 用户提交：固定内容、范围、问题、目标 sessionId
  → Harness 会话日志与 Agent：唯一执行和持久化所有者
  → 事件投影：响应、错误、取消、恢复游标
  → Lens / Harness UI：同一会话的不同视图
```

### 4.2 所有权

| 部件 | 负责 | 不负责 |
|---|---|---|
| Provider | 从单一来源采集并报告可用能力 | 会话创建、模型调用、对话持久化 |
| CaptureRuntime | 事件合并、采集取消、暂停、快照失效 | 推断用户同意发送内容 |
| Bridge | 消息关联、长度验证、权限策略、传输恢复 | 决定模型如何回答 |
| SelectionContext | 临时快照、当前选择、固定引用的解析规则 | 代替会话保存模型输入 |
| Session 适配插件 | 经宿主 API 提交、记录、订阅和停止 | 直接编辑宿主日志文件 |
| Lens UI | 用户意图、上下文预览、回答显示、焦点操作 | 自行编排第二套 Agent 循环 |

### 4.3 核心状态

采集状态建议为 `active / paused / unavailable / error`，连接状态为 `disconnected / connecting / connected / recovering / error`。两者独立：主动暂停不能被自动重连取消，关闭 Lens 不等于暂停采集或取消请求。

Lens 状态建议为 `hidden / affordance / composing / submitting / streaming / completed / failed / cancelling / cancelled`。被动入口不得激活原生窗口；用户主动展开才转移焦点。生成中重新选择文字，仅更新下一次可用材料，不替换当前请求。

选区失效规则必须处理：取消选择、导航、关闭来源窗口、切换文档、TTL 到期和 Provider 不支持。`NotApplicable`、采集错误和“已确认无选区”不可混为一谈。打开 Lens 导致浏览器失焦时，不得误清除已固定选区；过期的固定材料可继续显示，但再次提交需要明确标记来源时间或要求重新采集。

### 4.4 接口设计必须解决的问题

1. `snapshotId` 与 `revision` 标识不可变材料；同一修订的数据不能被原地修改。跨进程 ID 在 TS 所有者处使用品牌类型，序列化时按协议转换。
2. Provider 实例、来源窗口/文档、采集代次和时间用于判断迟到消息，不能仅依赖系统时间生成全局唯一 ID。
3. 提交必须固定有效载荷及所选范围，日志不能只保留一个会过期的缓存 ID。日志应记录实际发送内容及来源，临时缓存和持久内容有不同保留规则。
4. 请求 ID、幂等键、会话 ID 和事件序号职责分开。声明幂等作用域和保留期；重启后不具备持久去重证据时，不得自动重发状态不明的提交。
5. 几何明确物理屏幕像素、WebView CSS 像素、DPI 和多屏坐标转换；允许负坐标。精确文字范围和外围元素范围需可区分，不能把近似值伪装为精确值。
6. 上下文扩展绑定材料修订与文档标识；来源变化时返回明确错误，不混合不同页面。大内容受字节和模型预算限制，不能突破既有帧大小限制。
7. 传输支持请求回复与异步事件。不能让每个请求各自读取同一字节流并误消费其他请求的回复；采用单一读取分发与请求追踪。
8. 错误至少覆盖不支持、无选区、过期、来源变化、协议不兼容、超时、断线、取消和提交状态未知。错误码由 T01 统一定义，用户文案由本地化字典拥有。
9. Named Pipe 的同用户访问要求需要真实实现和负例测试。管道名称不是认证；是否需要额外配对凭据取决于明确威胁模型，不能声称握手已经认证客户端。

## 5. 任务依赖与里程碑

| 任务 | 名称 | 前置 | 主要所有者 | 里程碑 |
|---|---|---|---|---|
| T00 | 可复现开发基线与证据约定 | 无 | 集成 | M0 |
| T01 | 状态、协议及共享测试契约 | T00 | 协议维护者 | M0 |
| T02 | 采集可靠性与隐私控制 | T01 | Native | M1 |
| T03 | IPC 生命周期与事件分发 | T01 | Bridge | M1 |
| T04 | Lens、几何、焦点与输入 | T01；集成依赖 T02/T03 | UI + Native 窗口 | M1 |
| T05 | Harness 会话与 Agent 完整闭环 | T03；UI 集成依赖 T04 | Harness | M2 |
| T06 | 按需上下文与来源核查 | T02/T05 | 上下文 | M2 |
| T07 | 设计系统、可访问性与视觉完善 | T04；最终验收依赖 T05/T06 | UI | M2 |
| T08 | 可选 Provider 与支持矩阵 | T02/T06 | Provider | M3 |
| T09 | 性能、理解效果与人因评估 | T05/T06/T07 | QA + 用户研究 | M2/M3 |
| T10 | 安装、诊断、升级与卸载 | T03/T07 | 发布 | M3 |
| T11 | 集成验收与发布候选 | T00–T07、T09/T10；纳入的 T08 | 集成 | M3 |

M0 表示可复现且接口确定；M1 表示稳定采集和可操作 Lens；M2 表示浏览器 AI 闭环及初步效果证据；M3 表示明确支持范围内可安装、可恢复、可审查的候选版本。M2 不等于所有扩展 Provider 完成，M3 不等于已经获得公开发布授权。

## 6. 分阶段开发任务

### T00：可复现开发基线与证据约定

**目的。** 确保后续所有开发、在线审查和本地测试针对同一代码与依赖，避免在错误目录操作或使用不属于目标仓库的命令。

**范围。** 开发说明、工具链锁定、锁文件、CI 基础工作流、测试结果模板和最小 Agent Note 约定。不新增产品功能，不重构 Provider，不改变已安装 Harness profile。

**实现方式。**

1. 确认 Git 远端、当前分支、HEAD 和未提交修改；目标不存在时在独立目录获取检出。
2. 核对 `package.json`、各 workspace 脚本、Rust/Tauri 配置及 Node/pnpm/Rust 要求；记录实际可工作组合及 Harness 宿主版本，不凭 README 假定环境可用。
3. 在干净状态生成适用的 pnpm 锁文件及原生应用 Cargo 锁文件；锁文件中不包含私有源凭据。决定工具链与依赖缓存键。
4. 建立 GitHub Actions：可移植 TS 检查与 Windows Rust/构建检查分开。实际 UIA 测试需要交互式 Windows 桌面，普通无头 Runner 不能标记为已覆盖。
5. 在开发说明中给出目录明确的 Windows PowerShell 命令；禁止把 POSIX 环境变量赋值语法直接复制为 PowerShell 指令。
6. 建立测试证据文件格式和任务状态表。仓库已有同等机制则复用，避免重复文档体系。

**测试方式。** 以下是待开发者执行的现有命令，不是本文编写时的执行记录。

```powershell
git remote get-url origin
git status --short
git rev-parse HEAD
node --version
pnpm --version
rustc --version
cargo --version
pnpm install
pnpm check:task5
cargo fmt --manifest-path native/src-tauri/Cargo.toml -- --check
```

锁文件提交后，第二个干净检出使用 `pnpm install --frozen-lockfile` 复验；Cargo 检查使用 `--locked` 验证原生依赖可复现。主仓库的 `pnpm run doc-sync` 不属于目标插件的现有命令，不应照搬。

**验收与交付。** 提交工具链记录、依赖锁文件、CI 定义、`docs/development.md` 与证据模板或仓库内等价路径。检查真实通过或清楚列出阻塞；不得把安装网络失败记作产品失败，也不得忽略构建失败继续宣告基线就绪。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T00，并遵守第 1 节公共约束。目标仓库是 mivaille777/Harness-plugins，不是 DeepSeek Harness 主仓库。
先确认远端、分支、HEAD、未提交修改和适用 AGENTS.md；若当前目录不是目标检出，定位或获取独立检出，不修改当前无关目录。
核对实际包脚本、Node/pnpm/Rust/Tauri 与 Harness 宿主依赖，建立可复现锁文件、最小 CI、开发说明、决策记录约定及测试证据模板。只实施基础设施，不新增采集或 UI 功能，不改变用户已安装的 profile。
执行 T00 的现有检查，并在干净检出验证冻结依赖安装。区分编译、模拟测试、交互式 Windows 实测和真实模型测试；环境缺失时记录未执行项，不伪造通过。
完成后报告变更文件、可工作工具链、实际命令和退出码、剩余阻塞、T01 的准备状态。不要自动推送、合并或发布。
```

### T01：状态、协议及共享测试契约

**目的。** 在并行开发前确定 TS、Rust、UI 与 Harness 对选区、请求、错误和订阅的共同理解。

**范围。** 协议类型、验证器、JSON fixture、状态说明、配置说明和协议测试。不接通模型，不实现完整 Lens，不给尚未落地的能力返回成功。

**实现方式。**

1. 按第 4 节明确当前材料、固定材料和提交材料的关系，以及暂停、断开、关闭、取消的区别。
2. 核对已有协议的预留消息，优先补齐语义；R03 的必填持久回执已经将协议升级为 V2。后续需要不兼容变更时继续采用显式版本策略，同时更新 TS/Rust 和所有 fixture。无兼容承诺不等于忽略版本协商。
3. 定义选区失效、采集状态、订阅事件、取消、上下文来源及错误字段；明确空值、缺失、上限和时间语义。
4. 定义请求 ID 与幂等键、回复关联、事件序号、重连游标、重复/乱序事件和未知提交状态处理。
5. 为状态转换编写可执行测试。将几何坐标系与精度标记写入类型和 fixture；验证负屏幕坐标、非法尺寸、Unicode 和过大输入。
6. 维护一个共享 fixture 来源。若适合引入 Schema/代码生成，在同一变更中提供生成与陈旧检查；不要为了减少几个类型引入庞大生成系统。

**测试方式。**

```powershell
pnpm test:protocol
pnpm test:selection
pnpm test:rust
```

拟新增 `pnpm test:contract`：运行 TS/Rust 对同一正反例的接受/拒绝测试、协议能力与实现状态检查；fixture 为空、任何一方未执行或非法消息被接受都必须失败。

**验收与交付。** 正例同时通过，反例同时拒绝；路由只公告已实现能力。输出协议决策记录、共享 fixture、状态转换清单及新增检查脚本。协议审查通过后才允许下游依据新字段并行实现。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T01；先核对 T00 已验收，遵守公共约束并读取现有 TS/Rust 协议、验证器和共享 fixture。
统一选区生命周期、固定快照、提交幂等、异步事件、取消、错误、几何坐标和能力协商语义。复用已有消息；新增字段或版本必须同时更新两端及正反例。禁止将预留但未实现的能力公告为可用。
实现 test:contract 并接入明确的 CI 路径，证明正例两端接受、非法输入两端拒绝、空测试和漏执行不能通过。运行 T01 所列的现有测试及新增测试。
本任务不接入 LLM、不实现完整 UI。交付协议与状态说明、配置约定、代码与 fixture、测试证据及下游接口清单。不要自动推送、合并或发布。
```

### T02：采集可靠性与隐私控制

**目的。** 让系统始终知道选区是否有效，确保暂停可信、材料新鲜、采集失败不会干扰阅读。

**范围。** Native CaptureRuntime、默认 UIA Provider、SelectionContext 及采集状态投影。不实现任意应用支持，不增加自动模型调用，不使用模拟 Ctrl+C 作为默认方案。

**实现方式。**

1. 使用有界通道和最新值合并控制采集结果；用户已提交请求不能套用“最新值覆盖”的选区规则。
2. 区分选区不存在、Provider 不适用和采集失败；按 T01 发布生命周期信息，处理取消选择、页面切换、来源窗口关闭与过期。
3. 暂停状态独立于连接。用户暂停后撤销或忽略采集触发，清理待采集内容；重连不能恢复采集。定义重启后暂停偏好的保留方式。
4. 在正确的 COM/线程所有权下复用 UIA 会话。所有线程、事件注册、异步任务拥有退出路径；遇到不可取消 UIA 调用时采用可隔离策略，不承诺无法兑现的强制取消。
5. 将采集稳定等待、去重窗口、上下文长度、缓存容量和 TTL 接入拥有者配置，校验非法值。
6. 添加无内容的性能计数：采集时延、丢弃数量、队列深度、错误类型。默认诊断不输出完整选中文字或含敏感参数的 URL。
7. 过滤密码/受保护控件。按可观测能力提供应用/站点排除；无法可靠识别隐私窗口时明确能力限制，不能声称自动排除全部隐私窗口。
8. UIA 事件缺失的备用触发先通过实测证明必要。若增加快捷键或事件回退，保持主动可控且不引入高频全桌面扫描。

**测试方式。**

```powershell
pnpm test:selection
pnpm test:browser-accessibility
pnpm test:rust
```

拟新增 `pnpm test:capture`：生命周期与容量自动化测试；`pnpm test:capture:windows`：有交互桌面的实际 UIA fixture 测试。后者必须检测平台/桌面前提，缺失时清楚标记不可验收，不得靠空跑成功。

**必测场景。** 快速 A→B→C、同文字不同来源、取消选区、来源关闭、Lens 获得焦点、暂停后继续选择、页面无 UIA 事件、UIA 异常、中文与 emoji、超长选择、低性能页面、工作线程退出。清除当前选区不能破坏已固定或已提交的材料。

**验收与交付。** 固定材料不会串换；用户暂停后不产生新采集更新；队列长度有上限；结束进程后无残留工作线程或监听；真实 Chrome/Edge 无扩展路径成功。提交可复现测试页面、支持矩阵和运行证据。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T02，先核对 T01 的协议和状态定义。限定修改 CaptureRuntime、默认 UIA Provider、SelectionContext 和必要的状态投影。
实现有界采集、最新值合并、明确选区失效、用户暂停、线程与监听清理、可验证配置及最低必要的隐私控制。区分无选区、不支持、错误和 Lens 主动抢得焦点后的固定材料；不要误清除已提交上下文。
保持默认无浏览器扩展、无剪贴板写入、无模拟复制粘贴，不扩大到任意桌面应用，不调用模型。
新增并执行 test:capture；为 test:capture:windows 实现真实 UIA fixture 与环境检测，结合 Chrome/Edge 验证。不能把辅助函数测试冒充实机测试。
交付实现、配置文档、支持矩阵、决策记录、实际测试结果和未验证的 OS 行为。不要自动推送、合并或发布。
```

### T03：IPC 生命周期与事件分发

**目的。** 消除无响应连接导致的挂起，并为流式会话事件提供可靠传输。

**范围。** TS Named Pipe 服务端、Rust 客户端、请求追踪与诊断状态。用户暂停语义由 T01/T02 拥有，本任务不得另造状态。

**实现方式。**

1. 为连接、握手、读写和请求完成设置明确时限；每个超时释放请求、任务、锁和相关资源。
2. 用单一读取循环解析数据并按请求 ID/事件类型分发。写入需要序列化和背压，不能在等待响应时阻塞全部状态查询或取消动作。
3. 支持半包、多帧、过大帧、未知版本、断开、重连与服务重启。协议不同类型的载荷分别校验。
4. 自动恢复采用有限、可取消的退避策略；用户主动断开或暂停后不触发违反其意图的恢复。
5. 将连接、采集、宿主能力分别推送给 UI，避免只在按钮操作后刷新状态。
6. 提交操作区分“未写出”“等待回复”“结果已知”和“状态未知”。只在满足幂等约定时自动重试；不要把传输失败等同于服务器未执行。
7. 审查 Windows 管道访问权限及多客户端场景，为支持的访问隔离提供负例证据。若 Node API 不足以设置所需 ACL，选择维护良好的系统封装并记录适配成本。

**测试方式。**

```powershell
pnpm test:protocol
pnpm test:bridge
pnpm test:rust
```

拟新增 `pnpm test:bridge:integration`，在真实 Windows Named Pipe 上运行 TS/Rust 进程链路。测试进程须有清理机制和超时，遵循宿主允许的应用启动方式。

**必测场景。** 服务端不回包、收到半帧后断开、握手拒绝、超大帧、事件插入请求回复之前、两请求并发、重复回复、服务重启、主动断开后采集、发送后断线的未知提交状态、未授权身份访问、多客户端竞争。

**验收与交付。** 无无限等待；所有请求有终态或明确未知状态；异步事件不会被当成请求回复；断线恢复不造成重复模型提交；泄漏测试通过。提交故障注入测试、状态投影及权限说明。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T03，依赖已合并的 T01，不改写 T02 的暂停语义。
重构或补齐 Named Pipe 的明确超时、可取消恢复、单一读取分发、请求关联、事件推送和背压。不要在持有全局锁等待网络回复时阻塞状态查询或取消。明确状态未知的提交如何查询，不能盲目重发。
审查并实现声明范围内的管道访问隔离，使用真实负例证明；管道名称和普通握手不能当作身份认证。
运行现有协议、Bridge 与 Rust 测试，新增 test:bridge:integration 验证真实 Windows 跨语言管道及故障清理。环境不足时保留未验收状态。
交付代码、故障矩阵、配置说明、权限证据和下游会话事件接口。不要自动推送、合并或发布。
```

### T04：Lens、几何、焦点与输入

**目的。** 将采集能力转为用户可发现、可控制的阅读入口，确保浮层不打断选区和输入。

**范围。** Lens 状态机、原生窗口、精确或明确降级的几何、React 输入和模拟回答容器。不私自接入模型；T05 未完成时只能显示明确的开发模拟状态。

**实现方式。**

1. 区分被动入口与主动展开窗口；实现不激活入口、用户主动展开、关闭后恢复来源窗口焦点。焦点恢复前验证来源窗口仍存在且用户没有切换到其他任务，避免抢回焦点。
2. 读取选区范围矩形；现有 Rust 包未暴露能力时评估维护良好的系统绑定或最小封装。精确矩形不可用则采用有标签的近似定位或固定侧边位置，不伪造精度。
3. 明确 DPI 转换及负坐标，选择所在显示器工作区，避开选中文字、鼠标拖选路径、系统任务栏和屏幕边缘。跨行选择使用范围集合或定义清楚的代表锚点。
4. 打开 Lens 时固定 `snapshotId + revision` 并保留来源预览。新选区以“可切换材料”显示，不能覆盖当前草稿；重新提交前展示将发送的材料。
5. 提供解释、自定义问题、上下文范围、目标会话和关闭操作。无法使用的能力应隐藏或说明原因；不得出现空按钮。不得提供翻译入口。
6. 明确 Enter/Shift+Enter/Esc 语义。中文输入法 `isComposing` 时不发送；键盘可进入入口，焦点顺序可预测。
7. Esc 关闭 Lens 默认不停止正在运行的任务；停止生成使用独立动作并显示状态。关闭后可从托盘或快捷键恢复同一任务视图。
8. 使用 T01 fixture 驱动 UI 开发；把连接诊断迁入独立页面，产品 UI 不显示任务编号与协议细节。

**测试方式。**

```powershell
pnpm test:native-ui
pnpm --dir native build
```

拟新增 `pnpm test:lens`：状态转换、固定材料、定位算法、输入法和键盘组件测试；`pnpm test:lens:e2e`：真实 Windows Tauri 窗口的焦点与屏幕几何验收。普通浏览器中的 React 测试只能覆盖 WebView 内容，不能承担原生窗口验收。

**手工与自动矩阵。** Chrome/Edge × 鼠标/键盘选区；100%/150%/200% 缩放；主屏与负坐标副屏；横竖屏；跨行、长文、窗口边缘；浏览器缩放与显示器 DPI 同时改变；中文输入法；来源关闭；生成期间重新选择；Esc 后恢复原文。关键平台案例必须保留截图、事件记录或人工签字结果。

**验收与交付。** 被动入口不抢焦点、不发模型请求；固定材料不串换；没有 off-screen 控件或无法关闭的窗口；输入法确认不误发送；关闭/停止语义一致。提交 UI 状态图、几何与焦点实现、测试 fixture 和平台证据。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T04。先核对 T01；可以用共享 fixture 开发 UI，但最终集成必须核对 T02/T03。
实现被动不激活入口、主动 Lens、选区固定、来源预览、基础输入与目标会话入口。解决精确/近似几何、多 DPI、负坐标、多屏边缘、焦点恢复、Esc、中文输入法和关闭与停止的不同语义。
保持用户草稿和已固定材料不被新选区悄悄覆盖。模型能力未就绪时不得伪装真实回答，不增加第二套 LLM 客户端。诊断细节移出主要用户流程。
新增 test:lens 与 test:lens:e2e，分别验证组件行为与真实 Windows Tauri 窗口。用实际环境验证焦点和几何，不能只提交浏览器截图。
交付实现、状态图、实际测试证据、平台限制和 T05 对接点。不要自动推送、合并或发布。
```

### T05：Harness 会话与 Agent 完整闭环

**目的。** 让用户从选区直接获得可追问的真实回答，并保持单一、可回放的会话历史。

**范围。** Harness 会话适配插件、模型输入记录、选区工具、会话列表/创建/提交/订阅/停止，以及 Lens 回答投影。默认不修改 Agent Loop。

**实现方式。**

1. 读取目标 Harness 版本实际的 SessionController、工具注册、日志和事件接口；先做最小集成验证，再确定适配实现。源码中未发现的 API 不得凭名称编造。
2. 将会话适配作为 Consumer，通过依赖服务接入。缺少宿主能力时报告不支持，并从能力公告移除对应操作；自包含配置错误应尽早失败。
3. 首先接通用户选择已有会话或显式创建会话。解释模板、用户问题、固定快照和范围共同形成输入；所有实际模型可见内容通过宿主合法路径记录。
4. 会话缓存不是完整历史。提交前原子地取得材料并写入合法会话输入；若日志只能保存缓存引用，则尚未满足持久化要求。
5. 定义 `queue` 与 `steer` 对当前宿主的实际含义。默认采用不会意外中断现有任务的模式；宿主不支持某模式则禁用，不用近似操作伪装。
6. 注册 `selection_current` 与 `selection_read_context` 或经审阅确定的工具名。工具使用当前会话绑定材料；读取全局最新材料必须遵循显式规则，不能使无关会话意外读取用户新选区。
7. 实现请求幂等、提交结果查询、事件序号、订阅恢复与取消终态。事件在 UI 中按 sessionId/requestId 过滤，迟到片段不覆盖新任务。
8. 原文、网页和文档内容作为引用材料，不赋予指令优先级。来源中嵌入的“忽略规则”等文字不能更改权限或触发外部操作。
9. 使用宿主提供的工具审批、人机交互和错误机制。Lens 展示待用户操作状态，不绕过审批，也不再造独立审批数据库。
10. 若公开会话事件或 Agent Loop 改动确实必要，建立单独宿主 PR，更新该版本要求的 TS/Python SDK 预期输出和无密钥会话快照；插件 PR 记录依赖版本。

**测试方式。** 复用现有 `pnpm test:protocol`、`pnpm test:bridge` 和 `pnpm test:native-ui` 作为相关回归。拟新增：

```powershell
pnpm test:session
pnpm test:session:replay
pnpm test:session:e2e
```

`test:session` 验证适配与状态；`test:session:replay` 使用无密钥记录回放验证实际模型输入、工具输出与 UI 投影；`test:session:e2e` 通过支持的 `dsh` profile 执行真实宿主和真实模型测试。密钥缺失可明确跳过最后一项，但不能标记真实链路已验收。若新增宿主改动，宿主检查在其独立目录执行并单独记录。

**必测场景。** 解释、上下文追问；同一材料不同会话；提交后 TTL 到期；生成期间新选区；重复点击；写出后连接断开；恢复订阅；取消与完成竞态；模型错误；工具待审批；恶意网页指令；清除临时缓存后历史仍可回放。

**验收与交付。** 用户从真实浏览器选区收到真实模型回答，并能在完整 Harness 会话看到相同历史。重启后的模型输入可重建；重试不重复执行；不同会话不串材料和回答。提交适配代码、工具说明、回放 fixture、实际 e2e 证据和宿主版本约束。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T05，核对 T03，最终 UI 闭环依赖 T04。先读目标 Harness 版本的实际会话、日志、工具和事件 API，禁止凭协议名称假定宿主实现。
实现合法插件形式的会话适配、会话选择/创建/提交/订阅/停止，以及选区工具和 Lens 事件投影。所有模型可见材料必须能从 Harness 持久日志重建；Native 不调用 LLM、不直接写日志、不维护第二套历史。
明确 queue/steer、幂等保留期、未知提交状态、恢复游标、取消和迟到事件。防止新选区或其他会话污染当前请求。网页正文作为数据，不能覆盖系统指令或审批规则。
新增 test:session、test:session:replay、test:session:e2e；前两者无密钥可运行，第三者按真实环境和授权执行。缺少密钥或宿主能力时报告未验证，不能以模拟成功代替。
必要宿主改动单独限定范围并满足架构、Agent Note、SDK 和快照规则。交付完整用户闭环证据、回放材料、依赖说明及实际检查结果。不要自动推送、合并或发布。
```

#### T05 后续执行清单与核查修订

**当前执行入口（2026-09-08）。** 后续 Codex 先读 [后续 Codex 开发任务指南](docs/codex-next-development-guide.md)，再读 [DeepSeek Harness 完整交互开发计划](docs/harness-integration-development-plan.md)。前者把当前状态、R05～R08 的目标、边界、实现方式、验收命令、截图规范、并行分工与可复制提示词整理为直接可执行的工作包；后者保留完整技术设计和证据门槛。两份文档均以当前工作树和已安装 Harness 版本为准，不能覆盖后续已合入的工作。

当前工作树已完成 Harness 服务依赖声明、会话创建/恢复、`queue` 与 `steer` 提交、请求幂等、取消消息、协议层订阅、Lens 固定材料提交，以及 R06 的 durable history 分页、Native list/create/history/unsubscribe、Lens 会话选择和恢复投影。R07 运行器现在提供隔离 `dsh --profile headless` 的 L1 无密钥 profile smoke，可通过 `R07_DSH_REPOSITORY` 固定用户指定的 Harness 源码 checkout，并提供只接受真实 Tauri/浏览器驱动报告的 L3 验证入口。2026-09-09 已在 `D:\deepseek harness\deepseek-harness` 的 `0.1.1-rc.2`、`b150a551b8d465e31e418e1b2eaf5e79bbb7d28e` 上验证 headless 安装、bundle 合成、插件加载、本地确定性回答和 durable session 文件；也验证了隔离 web profile 的配置层、服务 URL、插件加载和 Named Pipe 空选区响应。真实模型、宿主审批、真实浏览器选区和可见 Lens 窗口仍需后续任务验证。源码 checkout 首次验证先运行 `pnpm install`、`pnpm build:lib:host`；Web 还需 `pnpm build:lib:client` 与 `pnpm build:web`。每项独立提交，不能把“请求已入队”写成“已得到回答”。

| 子任务 | 目的与完成定义 | 实现边界 | 自动验证 | 实机验证 |
|---|---|---|---|---|
| T05-A | 在同一 session 中可靠取得持续事件。`session.subscribed` 必须先于任何重放事件，连接关闭后所有订阅释放。 | Native 使用独立订阅管道或经审阅的单连接多路复用器；请求回复和 `agent.event` 由 request id/帧类型分派，禁止两个读取者竞争同一管道。TS server 维持写入顺序并在 socket close 时释放 disposer。 | 新增 `pnpm test:session:transport`：响应先行、分片帧、多订阅、关闭释放、错误帧、超时。保留 `pnpm test:bridge`。 | Windows 命名管道上连续发送 100 个受控事件，无串帧、泄漏或悬挂。 |
| T05-B | 将每个 Lens 请求与其所属 turn/回答严格关联。迟到片段、旧 session、取消后事件都不得覆盖新回答。 | 设计并持久化 request-to-message/turn 关联；只能基于宿主事件中可重建的事实关联。若 `0.1.1-rc.2` 没有足够事件字段，停止在插件侧推断，改为独立 Harness 宿主改动。恢复订阅使用已确认 cursor。 | 扩展 `pnpm test:session:replay`：双 session、重复 requestId、取消/完成竞态、重连 cursor、TTL 到期及异常恢复。 | 生成时切换选区、关闭再打开 Lens、断开 bridge 后重连，核对回答未串料。 |
| T05-C | Lens 显示回答、运行状态、错误和停止入口，且保留用户草稿和固定快照。 | 新增显式 `idle/submitting/streaming/completed/cancelled/error/approval-required` 状态；仅在 sessionId 与 requestId 匹配时追加可见文本。停止调用 `session.cancel`，不影响采集暂停。流式文本维持用户滚动位置，aria-live 仅播报状态转换。 | 新增 `pnpm test:lens:session`：中文输入法、双击、停止、迟到事件、错误重试、焦点与 Esc。运行 `pnpm test:native-ui`。 | Narrator、125%/200% DPI、窄窗口、长回答及中英文输入下完成解释、追问、停止。 |
| T05-D | 向 Agent 提供会话绑定的选区工具，并保证模型的工具读取不会拿到其他会话的新选区。 | 在 Agent 创建/恢复时将不可变 snapshot 绑定到该 Agent scope；注册审阅后的 `selection_current`/`selection_read_context` 工具。工具只读取该 session 的持久材料；范围扩展留给 T06。使用 Harness 审批与工具结果日志。 | 新增 `pnpm test:session:tools`：跨 session 隔离、快照清除后重放、工具审批、恶意网页文本。 | 真实 profile 中核对工具调用、审批 UI 与 session log 可重建输入。 |
| T05-E | 取得可审查的真实闭环证据。 | 使用隔离 `DSH_HOME`、已安装的打包 bundle、非敏感固定网页 fixture 和显式配置的模型凭据；不改动用户日常 profile。保存命令、版本、SHA、日志与截图路径，密钥不入库。当前已交付 L1 profile smoke 和 L3 严格驱动报告协议；真实层仍需实机执行。 | `pnpm test:session:e2e` 的 L1 无密钥路径可运行；`pnpm test:r07:runner`、`pnpm test:lens:runner` 覆盖运行器；真实模型和 L3 前置缺失时以明确 NOT RUN/exit 2 报告。 | 浏览器选区 → Lens 解释 → 回答 → 追问 → 完整 Harness 会话历史 → 重启后回放；每项记录 PASS/FAIL。 |

**2026-09-09 当前状态。** R01 已在 `1f5107b` 完成回答投影自动检查；R02 已在 `c2e685e` 完成连续订阅自动检查及实际 Windows Named Pipe 验证；R03 已在 `00ccb6a` 完成请求身份、并发幂等、未知提交恢复、取消竞态和 Protocol V2 自动检查及 Windows 管道验证。R04 已将提交材料升级为 Protocol V3，完成 durable 材料、Agent-scoped `selection_current`/`selection_read_context`、同快照安全重试、TS/Rust fixture、真实 ToolRuntime 测试和实际 Windows Named Pipe 验证；其状态为“自动检查与 Windows 管道通过待实测”，详见 `docs/evidence/r04-session-bound-selection-tools.md`。R05 的 rc.2 宿主 API 审计证明缺少可恢复、可竞答的 pending interaction capability；插件不以本地状态伪造闭环，先完成 `docs/host-tasks/r05-durable-session-interactions.md`。R06 已完成 durable history 投影与分页、列表/创建/读取/取消订阅命令、Lens 会话选择和恢复订阅的自动检查与 Windows 管道验证，详见 `docs/evidence/r06-session-history-implementation.md`。R07 已把 `test:session:e2e` 从无条件退出 2 改为隔离 profile 运行器（L1 本地确定性 provider PASS，L2 真实模型 NOT RUN），并把 `test:lens:e2e` 改为严格的真实 Tauri/浏览器驱动报告入口（当前 L3 NOT RUN）。R08.1/R08.2 已交付有界上下文预览、感知优先 UI、无障碍/视觉检查和 Tauri 截图；R08.3 已交付本地性能与匿名研究基础设施；R08.4 已交付安装元数据与脱敏诊断探针。真实模型、宿主审批、可见 Tauri 窗口和进程重启、真人人因、安装升级卸载仍待后续实机证据。T05 整体保持“开发中”。

**从当前状态到完整 DeepSeek Harness 交互的剩余交付包。** 每个交付包的完整步骤、文件边界、失败场景、命令和 Codex 提示词位于详细计划的同名章节；后续开发不得跳过前置验收。

| 顺序 | 交付包 | 目标 | 自动测试 | 必须补充的真实证据 |
|---|---|---|---|---|
| 1 | R04 会话绑定工具（自动验证通过） | scoped 注册 `selection_current`、`selection_read_context`，只读取当前执行请求的持久材料 | session:tools、replay、typecheck、contract、bundle 与 Windows Pipe 已通过 | 受支持 profile 的工具可见、工具结果日志和真实模型路径由 R07 验证 |
| 2 | H05 → R05 审批与 ask-user | 先发布宿主 durable interaction seam，再投影宿主待办并安全答复，不建立第二套审批状态 | H05 focused tests；发布后新增 session:interaction，加 tools、lens:session、protocol | 未批准前工具不执行；允许、拒绝、过期、另一客户端已处理均与宿主一致 |
| 3 | R06 会话与历史（自动验证完成） | 新建、选择、切换、分页历史和恢复订阅 | session:history、replay、lens:session、contract、Windows Pipe 已通过 | Lens 与 Harness 历史一致；真实重启不静默创建替代会话；完整导航入口仍待宿主 API |
| 4 | R07 真实闭环 | 把 session:e2e 与 lens:e2e 占位脚本改为分层、可失败、可清理的运行器；当前 L1 已通过，L3 驱动协议已具备 | r07 runner、lens runner、session:e2e、check:task5 | 浏览器选区→回答→追问→工具/审批→完整历史→重启回放；日志、截图和候选 SHA 对齐 |
| 5 | R08.1/R08.2 | 上下文范围、来源、安全渲染、中文、可访问性和高级视觉 | context-expansion、ui:a11y、ui:visual、Lens e2e | Narrator、125%/200% DPI、多屏、窄窗、长回答及完整状态截图 |
| 6 | R08.3/R08.4/R08.5 | 性能与人因、安装升级卸载、发布候选证据 | perf、human-factors、installer、check:release | 真人任务数据、干净 Windows 安装 smoke、同一候选 SHA 的兼容矩阵与证据索引 |

T08 的 Word、PDF 和其他应用 Provider 是可选交付包。未取得用户指定的目标应用和实际测试环境时保持“待开始”，不阻塞浏览器闭环，也不进入已支持列表。

**T05 完成门槛。** 只有 T05-A 至 T05-E 都有对应证据，且 `pnpm check:task5`、`pnpm test:session`、`pnpm test:session:replay`、新增会话传输/工具/Lens 测试通过，才可把 T05 状态改为“自动检查通过待实测”或“已验收”。没有真实模型与 Windows 证据时，状态保持“开发中”。

**下一位 Codex 提示词。**

```text
从 feat/t05-session-integration 的当前 HEAD 继续，不重置到旧基线。先读 docs/codex-next-development-guide.md、docs/harness-integration-development-plan.md、docs/evidence/r05-host-api-audit.md、docs/host-tasks/r05-durable-session-interactions.md、docs/evidence/r06-session-history-implementation.md 和 docs/evidence/r07-session-e2e.md；R01～R04 已完成规定的自动验证，R06 的历史、选择和恢复自动检查已完成，R07 已提供 L1 profile smoke 与 L3 严格驱动报告入口。
R01～R04 已形成投影、订阅、请求生命周期和会话绑定材料基础。rc.2 的 interaction、approval、permission 与 ask-user API 已完成只读探针，结论是缺少 R05 所需的 durable pending/atomic response 语义。不要创建本地待办表或第二个 provider；先完成独立宿主修改，再做插件适配与历史恢复。
先处理 H05 宿主 capability，再按 R07 接入真实模型 L2 驱动和真实 Chrome/Edge + Tauri L3 驱动。缺少凭据仍可运行 L1 框架和负例；真实调用缺前置时明确未验证。更新实际命令、退出码和证据，模拟、TCP、Named Pipe、真实模型及人工验收分别报告。L3 驱动必须写入 `DSH_LENS_REPORT_PATH` 并证明真实 Tauri 窗口、浏览器选区、必需状态和 PNG 截图，不能用静态页面代替。
```

### T06：按需上下文与来源核查

**目的。** 在降低信息缺失的同时控制发送范围，让用户理解回答依据并能主动补充材料。

**范围。** `selection / local / section / page` 范围解析、扩展请求、内容预算、来源与引用、缺失/截断 UI。默认浏览器和已有 Provider 能力优先；不承诺任何页面均可提取全文。

**实现方式。**

1. 按材料修订保存来源标识、采集时间、可用范围、实际范围和截断原因；不要仅用布尔值掩盖部分内容。
2. 用户主动选择范围后才执行更昂贵的扩展；模型工具请求扩展也必须受已授权范围约束。可配置默认范围，但不可静默扩大全文或截图权限。
3. Native Provider 接收带 snapshotId/revision/document 标识的请求，确认来源未变后采集；文档变化或范围失效返回明确错误并允许重新选择。
4. 字节上限、字符截断和模型预算在不同层分别检查。不能因传输允许 1 MiB 就将 1 MiB 内容全部送给模型；无法准确计数时使用保守预算并明确策略。
5. 原文引用与模型解释分开展示。来源标题、URL、位置和采集时间支持核查；链接协议验证，外部打开遵循用户操作；Markdown/HTML 输出采用受限渲染和清理。
6. 快照完整程度采用“仅选区/含邻近段落/来源缺失/已截断”等可理解标记。不要把现有启发式 confidence 显示为模型回答正确率。
7. 扩展失败保持原材料可用，让用户重试、缩小范围或继续明确标记的有限上下文回答。

**测试方式。**

```powershell
pnpm test:selection
pnpm test:protocol
```

拟新增 `pnpm test:context-expansion`：修订与来源一致性、范围授权、Unicode 截断、大小上限、扩展失败、恶意链接与输出清理；并更新 `test:session:replay` 验证实际送入模型的内容。

**验收与交付。** 发送预览与日志实际输入一致；页面切换不混料；无法获得全文不显示“已读取全文”；默认选择动作不触发页面扩展或模型请求；引用可核查。提交范围说明、预算策略、fixture 和用户界面证据。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T06，依赖 T02/T05。实现按用户意图选择的上下文范围、基于材料修订的延迟扩展、来源核查、明确预算与截断、失败降级和证据展示。
每次扩展必须验证仍是相同文档与选区，不能混合新页面。默认选区动作不触发全文读取或模型请求；模型工具也必须受已授权范围约束。记录模型实际收到的内容，不只记录缓存 ID。
将采集完整程度与答案正确率分开。受限渲染模型输出，校验链接协议，避免网页内容变成控制指令。
新增 test:context-expansion，并更新无密钥会话回放验证预览与实际输入一致。运行本任务相关现有回归，报告完整/部分/失败场景的实际结果。
交付实现、范围与预算说明、可核查 UI 和测试证据。不要自动推送、合并或发布。
```

### T07：设计系统、可访问性与视觉完善

**目的。** 在完整功能上形成一致、易读、流畅的交互，而不是用视觉效果掩盖状态和错误。

**范围。** 类型化设计变量、组件、双主题、本地化、空态/错误态、输出排版、键盘与辅助技术。不得改变 T01/T05 的业务语义，不顺带重写宿主前端。locale 只管理界面文案，不加入翻译产品动作。

**实现方式。**

1. 用一页设计规范确定色彩、对比度、字体、间距、圆角、阴影、动效、面板宽高约束和密度；从真实回答长度和多语言文字验证，不只做短英文样例。
2. 拆分入口、选区引用卡、范围选择、问题输入、回答、状态反馈与诊断组件。使用显式 props，避免 UI 组件直接读取全局“当前材料”。
3. 完成加载、首次使用、无选区、无能力、部分上下文、离线、失败、恢复、取消、等待审批和完成状态；每个状态有明确下一步。
4. 局部交互反馈与模型耗时分开；不展示虚假百分比或无依据剩余时间。流式回答保持布局和用户滚动位置，复制内容与显示文本一致。
5. 全部用户文案、aria-label、tooltip、时间/数字格式由本地化拥有，至少覆盖简体中文与英文。动态切换不丢失草稿和请求关联。
6. 验证可见焦点、Tab 顺序、Esc、屏幕阅读器名称、缩放、减少动画、高对比度和足够命中区域。避免每个流式 token 都触发 live region 播报。
7. 为截图基线固定字体、内容、窗口、主题、DPI、动画和时间输入；视觉基线必须人工检查，不能批量更新以消除失败。
8. 对 390 px 级窄窗为面板和控件设置可收缩的最小宽度，长状态文案允许换行，禁止用横向滚动隐藏主要操作；保留垂直滚动和渐进披露。

**测试方式。**

```powershell
pnpm test:native-ui
pnpm --dir native build
```

拟新增 `pnpm test:ui:a11y` 和 `pnpm test:ui:visual`。自动无障碍检查需要包含可失败的对比度、名称或焦点负例；屏幕阅读器及原生高对比度另行人工验证。运行 T04 的 Lens 原生 e2e 防止视觉重构破坏窗口行为。

**验收与交付。** 中英文、浅深主题、窄窗口、长回答无截断关键操作；自动检查无未解释的严重问题；键盘与 Narrator 可完成核心任务；视觉基线经人审。交付设计变量、组件、状态样例、截图与人工检查表，不用“高级感”替代可观察标准。窄窗回归还要验证 390×430 截图尺寸、会话选择器、状态通知和下一步按钮均在可视宽度内。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T07，基于 T04 的真实交互和 T05/T06 的最终状态，不改变会话或采集语义。
实现可读、克制的双主题设计系统和完整状态组件，产品流程只展示用户决策需要的信息。统一类型化本地化字典，覆盖中文/英文、无障碍名称、错误、日期数字及输入法。
确保长回答排版、稳定滚动、清楚引用、键盘操作、减少动画、高对比度和缩放。流式播报应适度，不允许每个 token 抢注意力。
新增 test:ui:a11y 与 test:ui:visual，运行现有 UI 检查与 Lens 原生回归；自动测试不能代替 Narrator 和原生焦点人工验证。不要通过无审查更新所有截图使测试变绿。
交付设计规范、组件、状态截图、本地化与可访问性证据，以及明确的未验证项。不要自动推送、合并或发布。
```

### T08：可选 Provider 与支持矩阵

**目的。** 在保持统一交互和来源准确的基础上扩展更多阅读材料，不破坏无扩展浏览器默认路径。

**范围。** 按独立子任务推进 DOM 扩展、通用 Windows UIA、Word COM 和 PDF 文本场景。每个 Provider 单独验收；未选择的子任务保持待定，不能用一个大 PR 混合全部平台。

**实现方式。**

1. T08-A 先验证现有 DOM 扩展：最小权限、消息来源校验、Native Messaging 帧格式、跨 frame 来源关联与按需页面扩展。安装和卸载均为用户可选动作。
2. T08-B 以有限应用清单验证通用 UIA，定义不支持控件和失败降级。继承 T02 的隐私过滤，不枚举所有应用来宣称普遍支持。
3. T08-C 根据实际安装的 Word 版本采用 COM Provider；显式管理 COM 线程、文档对象和引用释放。没有 Office 环境时只能标记未验收。
4. T08-D 对浏览器 PDF 文本、独立 PDF 阅读器和扫描 PDF 分开记录。先做可选择文本；OCR 若另行纳入，单独定义准确率、语言、坐标和权限要求。
5. 为同一选区多 Provider 命中制定确定性仲裁：用户授权、来源精度、能力、新鲜度优先顺序明确。不能混合 UIA 的文字与另一个标签页的 DOM 上下文。
6. 所有 Provider 通过共同契约输出快照、能力、来源、几何精度和失败信息；不能直接接入会话或另建传输。

**测试方式。** 现有 DOM 路径命令：

```powershell
pnpm check:browser-extension
pnpm --dir browser-extension exec playwright install chromium
pnpm check:browser-extension:e2e
```

拟新增 `pnpm test:providers`：共享契约、来源仲裁和降级的自动检查。Windows UIA/Word/PDF 真实支持矩阵由交互式环境验证，DOM fixture e2e 不等于实际安装扩展 + Native Messaging + Harness 全链路。

**验收与交付。** 每个被声明支持的应用都有版本、内容类型、限制和实测证据；未安装扩展时浏览器 MVP 正常；重复 Provider 不重复弹窗或提交；选择来源准确。提交单 Provider PR 和支持矩阵，不扩大无证据的支持声明。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T08，默认先做 T08-A；如果本次任务明确指定 B/C/D，则仅实施指定子任务。前置为 T02/T06。
复用统一 Provider、快照与传输接口，完善选定 Provider 的来源、范围、权限、仲裁、生命周期和降级。不得使浏览器 MVP 强制依赖扩展，不直接调用模型或维护会话。
实施 test:providers 的相应契约与仲裁检查，DOM 子任务执行现有扩展检查；实际应用能力必须在对应 Windows/Office/PDF 环境实测，不能用辅助函数或 DOM fixture 替代全链路。
对每个应用写清版本、内容类型、支持与不支持范围。缺少环境就记录未验收，不泛化为任意应用支持。
交付独立 Provider 变更、支持矩阵、安装/卸载说明及实际测试证据。不要自动推送、合并或发布。
```

### T09：性能、理解效果与人因评估

**目的。** 用证据确认产品降低操作和理解成本，同时避免速度提升以错误理解、误触或失控为代价。

**范围。** 性能测量、合成压力测试、可用性研究方案、匿名结果分析及发现的局部修复。不自动联系参与者，不采集生理数据，不把模拟用户结果当作真人证据。

**实现方式。**

1. 为选区稳定、入口绘制、用户提交、宿主接受、首个回答片段和完成分别记录单调时间；跨进程时钟差需通过明确的测量方法处理。
2. 性能 fixture 使用本地固定页面和确定性事件；真实模型耗时单独统计，不与 UI 反馈延迟混合。
3. 在参考 Windows 机器上定义硬件、浏览器版本、DPI、网络条件和采样数。初始建议：入口出现 P95 ≤300ms，按钮可见反馈 P95 ≤100ms；这些是暂定产品预算，不是外部标准或现有成绩。
4. 合成压力测试验证长时间运行、慢 Provider、连接停顿、事件洪峰和会话片段流。约束队列有界、无任务泄漏，RSS 不持续单调增长；绝对 CPU/RSS 预算在基线测量后记录，不能无依据宣称达标。
5. 准备 6–8 人形成性测试：术语解释、复杂段落理解、证据追问、恢复阅读、故障恢复。准备等难度任务并交叉安排“复制到聊天窗口”和“Companion”顺序。
6. 提前定义正确率评分 rubric 和关键错误。由评审者对匿名结果评分，不让被评估模型独自判定自己的答案正确；工作效率不能以理解正确率下降换取。
7. 记录完成率、时间、切换次数、误触、恢复时间、信任理解和 NASA-TLX。明确使用加权 NASA-TLX 还是未加权 Raw TLX，保持同一研究两种流程计分一致。
8. 研究需参与者知情同意；默认只保留任务 ID、时间和匿名评分。录屏、原文和模型答案若需要留存，单独获得同意并规定期限。
9. 对失败按阻断任务、误导材料、注意力干扰、效率和视觉问题分级，回到相应所有者任务修复并复测。

**测试方式。** 拟新增 `pnpm test:perf` 生成 JSON 统计、样本数、P50/P95 和环境元数据；`pnpm test:human-factors:fixtures` 验证任务材料、评分字段及匿名数据格式；`pnpm report:human-factors` 对真实录入的数据生成分析报告。报告命令必须要求明确输入文件，空数据不能生成“通过”的结论。

**初始验收目标。** 核心任务完成率 ≥90%；完成时间中位数相对基线下降 ≥25%；理解正确率不低于基线；未经用户主动发起的模型请求为 0；工作负荷和切换次数下降。上述效果目标用于首轮研究，不是通过小样本证明普遍有效的统计结论。未达到时分析原因并迭代，不删除困难案例；未招募到真人时将人因效果明确保留为未验证。

**验收与交付。** 输出可复现性能数据、研究材料、评分规则、匿名结果、局限和改进优先级。自动测试证明可靠性，真人任务证明理解与操作效果，两类证据分开。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T09，依赖浏览器 M2 闭环。先实现可复现性能测量和压力场景，再准备真人形成性研究材料。
分别测量入口、交互反馈、宿主接受和模型响应耗时；报告机器、样本、P50/P95、队列及资源趋势。新增 test:perf，不能靠缩短测量范围或隐藏慢样本达标。
新增 test:human-factors:fixtures 和 report:human-factors，明确输入文件、空数据失败、正确率 rubric、匿名字段和 TLX 计分方式。安排复制粘贴与 Companion 的交叉任务，但不要自动联系参与者或收集录屏。
有获授权的真人数据才计算效果；没有就交付待执行研究包，明确“人因效果未验证”，不使用 AI 模拟替代用户结果。
交付性能证据、研究包、真实数据分析或未验证说明、按严重程度排列的修复建议。不要自动推送、合并或发布。
```

### T10：安装、诊断、升级与卸载

**目的。** 让普通用户能够安装和恢复使用，并在停止使用时完整退出，不依赖开发终端长期运行。

**范围。** Tauri 打包配置、版本关联、诊断、托盘/恢复、首次启动、升级和卸载流程。默认不建设自动更新服务，不自动安装系统级启动项。

**实现方式。**

1. 核对 Tauri/Windows/WebView2 前提，启用实际安装打包；区分 npm Harness bundle 与 Native 安装器两个产物和责任。
2. 记录 Native、插件、协议和 Harness 支持版本。遇到不兼容版本显示可操作恢复说明，不静默继续。
3. 首次启动区分“未安装宿主”“插件不可用”“协议不匹配”“采集暂停”和“未选择材料”；用户不需要理解管道路径即可处理常见问题。
4. 提供托盘恢复与明确退出；退出释放采集线程、事件、连接和窗口。开机启动默认由用户选择，退出与关闭面板语义分开。
5. 安装默认路径与用户范围须明确；默认 UIA 路径不要求 Native Messaging 注册。可选扩展注册提供独立安装和卸载说明。
6. 提供脱敏诊断导出，包含版本、能力、错误码、时间和计数；默认不含选区原文、密钥、认证头或完整敏感 URL。
7. 在干净 Windows 用户环境验证安装、首次启动、升级、重新安装、卸载及宿主不可用。升级失败保留可恢复产物，不静默删除 Harness 会话数据。
8. 产物清单包含版本、SHA256、构建提交和已知限制。签名与公开分发只有获得对应授权和材料后执行，未签名候选明确标注。

**测试方式。** 现有构建入口如下，只有打包配置和前提完成后才可把输出当作安装候选：

```powershell
pnpm native:build
pnpm pack
```

拟新增 `pnpm test:installer`：检查产物、版本、安装/卸载探针及诊断脱敏；涉及系统变更的实测仅在获授权的隔离 VM/测试账户执行。自动安装器探针不替代首次安装人工流程。

**验收与交付。** 干净目标环境完成安装→启动→选区→提问→恢复→退出→卸载；常见失败可自助定位；没有残留采集进程和未经选择的启动项；卸载不删除宿主会话。提交候选产物、校验和、版本矩阵、安装记录和诊断样例。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T10，依赖 T03/T07。完成 Tauri 安装候选、npm bundle 的版本关联、首次使用诊断、托盘恢复、退出和升级/卸载流程。
保持 Native 与 Harness 会话职责分离。默认浏览器路径不安装扩展或注册 Native Messaging。开机启动由用户选择，不自动修改系统级配置；卸载不得删除 Harness 历史。
新增 test:installer，并在已授权的隔离 Windows 环境验证真实安装和卸载。执行 native:build 与 pnpm pack，记录产物、SHA256、提交和兼容版本。没有安装环境就明确未验收。
诊断默认脱敏，签名、上传和公开发布保持未执行，除非当次会话已有明确授权。交付候选产物、安装证据、版本矩阵、已知限制与恢复说明。
```

### T11：集成验收与发布候选

**目的。** 在同一个提交上证明声明支持范围内的功能、可靠性、交互和安装共同工作，避免各模块单测通过但最终产品断链。

**范围。** 发布检查聚合器、完整场景、证据索引、文档一致性和阻断问题修复。不以本任务自动授权远端合并或公开发布。

**实现方式。**

1. 冻结候选 SHA 和纳入的 Provider 支持清单；确认依赖任务的证据对应当前实现，变更后重测受影响场景。
2. 提供 `check:release` 聚合：默认浏览器自动检查、契约、会话回放、UI、性能和构建。交互式 Windows、真实模型、人因和安装证据用独立验收清单汇总。
3. 聚合器检查所需脚本和 fixture 存在，读取每个结果。缺少命令、空测试、未执行平台测试、仅 mock 的全链路测试都不能标记“完整验收”。
4. 执行固定黄金路径和故障路径。记录选区到日志再到 UI 的关联，允许公开报告使用脱敏材料。
5. 检查 README、帮助、设置页和支持矩阵是否与能力公告一致；移除开发模拟入口，保留明确受限的诊断工具。
6. 阻断缺陷包括材料串换、重复提交、私自模型请求、丢失无法恢复的输入、不可关闭/不可取消的操作、权限失效和不可安装。视觉细节缺陷可记录为已知问题，但不能掩盖可读性或关键操作障碍。
7. 输出候选审查包：提交、依赖、变更、功能矩阵、命令结果、人工证据、产物、校验和、未验证项与回退方案。未满足的要求保持待验收。

**测试方式。** T00 锁文件建立后执行：

```powershell
pnpm install --frozen-lockfile
pnpm check:release
git diff --check
```

`check:release` 已注册，但没有 manifest 时只生成待验收骨架并返回 NOT READY/退出码 2。它只证明被实际执行的自动检查；发布候选是否满足全部要求还取决于第 10 节人工和外部环境证据。

**黄金路径。** 无扩展 Chrome/Edge 选区→入口不抢焦点→主动解释→显示绑定来源的真实回答→追问→打开同一 Harness 会话→关闭返回原文→暂停→确认不再采集→退出→恢复运行。

**故障路径。** 无宿主、协议不兼容、来源关闭、材料过期、超大上下文、IPC 停顿、模型失败、提交结果未知、生成时换选区、取消/完成竞态、安装失败和升级后启动失败。

**验收与交付。** 支持范围内黄金路径通过，阻断缺陷清零，证据可追溯且候选可安装。没有真实人因数据时可以形成技术候选，但不得宣称已证明“感知增强”效果，也不得把第 10 节全部勾选。

**给 Codex 的完整提示词。**

```text
请执行《harness-plugins task.md》的 T11，先核对所有前置任务和本次纳入的 Provider 支持范围，冻结候选提交。
实现 check:release 聚合器，缺失命令、空 fixture、跳过关键测试或仅模拟链路不能产生完整验收通过。运行所需自动检查，再汇总真实 Windows、真实模型、人因与安装的独立证据。
完成黄金路径和故障矩阵，修复阻断问题并只重测受影响路径；所有证据必须对应最终代码。检查文档、能力公告、帮助与产物一致。
交付可审查候选包，列明功能、版本、SHA、产物、校验和、命令结果、人工证据、未验证项、限制及回退方法。技术候选与感知增强效果验证分开结论。
不要自动合并、签名、上传或公开发布，除非当次会话已明确授权对应动作。
```

## 7. 测试命令与证据规则

### 7.1 命令状态

本节的“已有”仅表示审阅基线中存在脚本定义，不表示在当前机器执行成功。“拟新增”表示负责该任务的 Codex 必须先实现脚本、实际测试与失败行为，再使用该命令。所有命令默认在目标 `Harness-plugins` 根目录的 PowerShell 执行。

| 已有命令 | 用途与限制 |
|---|---|
| `pnpm check` | TS 类型、根测试、构建和 bundle 检查；不等于原生全链路 |
| `pnpm check:task5` | 聚合根检查、Rust、原生 UI/前端构建及浏览器辅助检查；不是 UIA 实机证明 |
| `pnpm test:selection` | 快照与缓存行为 |
| `pnpm test:protocol` | 协议、分帧与请求追踪相关测试 |
| `pnpm test:bridge` | 当前以路由测试为主；不能仅凭命令名认定测试了管道 |
| `pnpm test:rust` | Cargo 单测；交互式 Windows 覆盖需单独核对 |
| `pnpm test:browser-accessibility` | 按名称筛选 Rust 测试；不是实际浏览器选择操作 |
| `pnpm test:native-ui` | React/UI 自动测试；原生 API 使用 mock 的部分另行说明 |
| `pnpm --dir native build` | 原生前端构建；不等于安装器 |
| `pnpm check:browser-extension` | 可选扩展检查及 Native Host 构建 |
| `pnpm check:browser-extension:e2e` | 可选扩展检查和 DOM e2e；实际安装与管道另验 |
| `pnpm native:dev` | 启动开发 Companion；持续进程不是一次性测试 |
| `pnpm debug:selection` | 构建并读取 Harness 当前选区；需要对应运行环境 |
| `pnpm native:build` | 原生构建入口；基线未启用安装打包 |
| `pnpm pack` | 打包 npm bundle，并执行 prepack 中的检查 |

### 7.2 命令责任表

| 命令 | 所有者 | 环境与测试对象 | 必须失败的情况 |
|---|---|---|---|
| `pnpm test:contract` | T01 | TS/Rust 共享协议与状态 fixture | 两端行为分歧、非法输入被接受、fixture 为空 |
| `pnpm test:capture` | T02 | 模拟源驱动真实采集状态组件 | 串换、队列无界、暂停失效、残留监听 |
| `pnpm test:capture:windows` | T02 | 交互式 Windows UIA 固定页面 | 已声明支持的选区未采集或来源错误 |
| `pnpm test:bridge:integration` | T03 | Windows TS/Rust 真实 Named Pipe | 超时失效、误关联、资源未清理 |
| `pnpm test:lens` | T04 | 状态、定位与输入组件 | 草稿串换、输入法误发、关键操作不可达 |
| `pnpm test:lens:e2e` | T04 | 实际 Windows Tauri 窗口 | 抢焦点、窗口越界、不能恢复阅读 |
| `pnpm test:session` | T05 | 宿主适配与请求状态 | 重复提交、会话串料、无终态 |
| `pnpm test:session:replay` | T05 | 无密钥宿主会话回放 | 无法重建实际模型输入、事件投影不一致 |
| `pnpm test:session:e2e` | T05 | 实际宿主 profile 与真实模型 | 主路径失败、历史或输入丢失 |
| `pnpm test:context-expansion` | T06 | 范围、修订、预算与渲染 | 混页、越权扩展、超限、危险链接执行 |
| `pnpm test:ui:a11y` | T07 | WebView 可访问性自动检查 | 未解释的严重可访问性问题 |
| `pnpm test:ui:visual` | T07 | 固定主题/窗口/语言视觉基线 | 未审查的布局与可读性差异 |
| `pnpm test:providers` | T08 | 共享 Provider 语义与仲裁 | 来源混用、重复触发、错误能力公告 |
| `pnpm test:perf` | T09 | 固定本地工作负载 | 无样本、超出已批准预算、资源持续泄漏 |
| `pnpm test:human-factors:fixtures` | T09 | 任务材料和匿名数据格式 | 缺评分项、材料不匹配、格式不合法 |
| `pnpm report:human-factors` | T09 | 显式传入的真实匿名数据 | 输入缺失、数据为空或无法计算指标 |
| `pnpm test:installer` | T10 | 产物及隔离 Windows 安装环境 | 产物缺失、版本不匹配、安装退出清理失败 |
| `pnpm check:release` | T11 | 自动发布候选检查聚合 | 任何必须检查失败、缺失或证据状态不明 |

新命令用适合 Windows 的脚本编排。需要环境变量时采用读取进程环境的 Node 脚本或 PowerShell 明确赋值，不在文档里复制密钥。以实际测试框架配置为准，不预设 Tauri 窗口可由普通 Playwright 页面驱动。

### 7.3 证据等级

| 等级 | 能证明 | 不能证明 |
|---|---|---|
| S：静态与构建 | 类型、包布局、产物可生成 | 用户路径和真实 OS 行为 |
| U：单元与组件 | 状态转换、边界、受控输入行为 | 实际 UIA、真实窗口和宿主集成 |
| I：跨组件集成 | 管道、宿主适配、事件回放 | 真人理解效果、未测试平台 |
| W：Windows 实机 | 选区、焦点、DPI、窗口与安装 | 模型质量和所有应用普适支持 |
| L：真实模型 | 真实宿主模型请求链路 | 普遍正确率和真人体验 |
| H：真人任务 | 样本中的效率、理解和负荷变化 | 超出样本的统计推广 |

### 7.4 默认手工联调顺序

T00 必须先在开发说明中确认目标 Harness 版本与 profile 启动方法。本计划保留仓库 README 的联调入口供核对，不声称已在当前机器验证。

1. 在独立测试 profile 安装经构建的 bundle；记录具体 profile 与产物。不得自动移除用户正在使用的插件来“重新安装”。
2. 在终端 A 启动该 profile 的 Harness。README 中的 `dsh web` 是待核对入口；使用实际宿主版本支持的命令，不伪造不存在的参数。
3. 在目标仓库终端 B 执行 `pnpm native:dev`。
4. 在无本项目扩展的 Chrome/Edge 打开固定测试页面并选择指定文本。
5. 在目标仓库终端 C 执行 `pnpm debug:selection`，核对文字、Provider、来源、修订和几何；此命令可能显示原文，只对非敏感测试材料执行。
6. T04/T05 完成后继续检查 Lens→提交→回答→追问→完整会话→返回原文。
7. 分别暂停采集、停止生成和关闭 Lens，核对三个动作的独立语义；结束后清理测试进程与临时 profile，保留用户数据。

### 7.5 失败与环境缺失的处理

- 新增的检查使用明确失败退出码；不要设置 `--passWithNoTests`、吞掉 Cargo 失败或无条件返回成功。
- 可选环境缺失可以显示 skip；但负责验收者必须把相应能力标记为“未验证”，发布清单不能据此通过必需项。
- 测试记录应包含实际命令及工作目录，不将计划命令列在“已运行”下。
- 快照失败先检查真实差异；仅在行为变更有意且经过审查后更新相关 fixture。
- 网络或工具链故障与产品断言失败分开记录；不得通过绕过产品权限或安全限制解决测试失败。

## 8. ChatGPT 与 Codex 并行工作流程

### 8.1 共同事实来源

GitHub 中的提交、接口 fixture、任务状态和测试证据是共同依据。ChatGPT 在线端与 Codex 本地会话不会自动同步记忆；连接器能读代码不代表它能运行本机 Windows、取得密钥或修改本地文件。每次交接都附 SHA，不仅写“最新代码”。

在线端适合需求、人因评审、状态设计、协议审查与 PR 差异分析。Codex 本地负责实现、构建、Windows 调试和真实环境验证。在线端若能编写改动，也使用独立分支；不能与本地工作在同一共享检出并发写相同文件。

### 8.2 并行分工

| 工作线 | 主要任务 | 文件所有权 | 交付给其他工作线 |
|---|---|---|---|
| 协议/集成 | T00/T01/T11 | 根配置、共享协议、fixture、CI、锁文件 | 已合并契约、基线 SHA、集成结果 |
| Native | T02/T03、T04 原生部分、T10 | `native/src-tauri/`，按模块再细分 | 状态事件、几何、窗口接口、安装证据 |
| Harness | T05/T06 | `src/context/`、会话适配及工具 | 宿主 API 适配、回放与请求状态 |
| UI | T04 React 部分、T07 | `native/src/`、UI 测试与样式 | Lens、输入、回答、状态与可访问性 |
| 扩展 Provider | T08 | `browser-extension/` 或单 Provider 文件 | 独立来源能力和支持矩阵 |
| 在线评审/研究 | T01/T07/T09 评审 | 任务说明、研究材料、审查结果 | 可执行验收和优先级意见 |

`src/bridge/`、TS/Rust 协议、原生入口、`Cargo.toml`、根包脚本和锁文件容易冲突，由集成维护者协调。多工作线需要改变共享接口时，先提交接口 PR，再分别实现 Consumer；不得各自复制一份接口类型绕过协调。

### 8.3 可并行与不可并行的部分

1. T00/T01 先完成并合并，形成共同起点。
2. T02 与 T03 可按采集/传输模块独立开发；T04 UI 同时用共享 fixture 实现。原生入口接线由一个所有者完成。
3. T05 在 T03 的事件与提交语义稳定后实现；前期可研究宿主 API，但不能合并依赖未定的协议实现。
4. T07 视觉可以与 T05 并行，但最终状态与真实回答必须在 T05/T06 合并后验收。
5. T08 子任务独立分支；T09 的研究材料可提前准备，实际效果测试依赖 M2；T10 可提前研究打包，最终安装证据依赖整合产物。
6. T11 在一个集成提交上运行，不能拼接不同 SHA 的通过记录宣告完成。

### 8.4 独立 worktree 示例

以下是后续开发者在目标 Git 检出中、确认工作目录及远端后执行的示例，不是本次已经创建的分支。分支已存在时选择现有正确工作区，不覆盖或删除。

```powershell
git fetch origin
git worktree add ..\Harness-plugins-native -b feat/capture-reliability origin/main
git worktree add ..\Harness-plugins-ui -b feat/selection-lens origin/main
git worktree add ..\Harness-plugins-session -b feat/session-integration origin/main
```

如果 T01 尚未合入 main，这些工作区仅可用于不依赖新协议的准备工作；开始实现前统一更新到已合并 T01 的提交。不要为了同步覆盖未提交代码。不同工作区使用不同测试端口、pipe 名称或 profile；实际产品默认值不因此改变。

### 8.5 在线审查提示词

```text
请通过 GitHub 连接器审查 mivaille777/Harness-plugins 的指定 PR/分支，参照仓库中的《harness-plugins task.md》。先读取并报告实际 base/head SHA，再读取完整变更和相关调用方；不要仅根据 PR 描述判断完成。
重点审查：任务范围、选区/会话/请求关联、用户控制、来源与隐私、失败恢复、真实测试证据和人因交互。将确定缺陷与待实测风险分开，指出对应文件和可复现条件。
检查新测试是否验证行为并能拒绝错误，而不是只验证实现细节；mock 测试不能证明 Windows UIA、焦点和安装成功。
输出阻断项、非阻断建议、缺少的验收证据及可交给本地 Codex 的修复任务。不要声称运行了本机测试，不自动发送评论、改代码、合并或发布。
```

使用者在同条消息附上实际 PR 链接或准确分支；缺少目标时在线端先定位相关任务状态，仍无法确定则请求唯一目标，不猜测审查其他分支。

### 8.6 本地接收审查意见提示词

```text
请在 mivaille777/Harness-plugins 的正确工作区处理本次附带的在线审查意见。先比较审查 head SHA 与当前 HEAD；如代码变化，重新验证问题是否仍存在。
按《harness-plugins task.md》对应任务范围修复可复现问题，保持原有用户改动。逐项记录已修复、不成立、未验证和需要用户决定的事项；不要为了满足过时意见回退正确行为。
运行受影响的行为测试及必要的 Windows 集成验收，更新对应文档和证据。完成后提供新 SHA 或当前未提交状态、变更摘要、实际命令、结果及复审重点。不要自动合并或发布。
```

## 9. PR、验收与任务交接模板

### 9.1 任务状态

状态使用 `待开始 / 开发中 / 自动检查通过待实测 / 待审查 / 已验收 / 阻塞`。每个状态带日期、所有者、SHA 和证据位置。“代码已写完”不是“已验收”；环境不足要记录阻塞条件和下一位验证者，不长期含糊标记完成。

| 任务 | 初始状态 | 所有者 | 验收 SHA | 证据 |
|---|---|---|---|---|
| T00 | 自动检查通过，待 CI | Codex | 本提交 | `docs/development.md`、`.github/workflows/ci.yml` |
| T01 | 自动检查通过，待 CI | Codex | 本提交 | `docs/protocol.md`、`pnpm test:contract` |
| T02 | 自动检查通过待实测 | Codex | 本提交 | `docs/capture-reliability.md`、`pnpm test:capture`、`pnpm test:native-ui` |
| T03 | 自动检查通过待实测 | Codex | 本提交 | `docs/bridge-transport.md`、`pnpm test:bridge`、`pnpm test:rust` |
| T04 | 自动检查通过待实测 | Codex | 本提交 | `docs/selection-lens.md`、`pnpm test:lens` |
| T05 | 开发中：R01 自动检查通过待实测；R02～R04 自动检查与 Windows 管道通过待实测；R05 宿主前置已识别；R06 自动实现完成；R07 L1 通过、L2/L3 未运行；R08.1～R08.4 自动检查通过待实测；R08.5 聚合器已实现但候选 NOT READY | Codex | R08.5 `c264c08ff41f4735553bb55da2015bc808bb7699` | `docs/evidence/r01-lens-session-projection.md`、`docs/evidence/r02-continuous-session-subscriptions.md`、`docs/evidence/r03-request-identity-and-recovery.md`、`docs/evidence/r04-session-bound-selection-tools.md`、`docs/evidence/r06-session-history-implementation.md`、`docs/evidence/r08-context-expansion.md`、`docs/evidence/r08-ui.md`、`docs/evidence/r08-performance-human-factors.md`、`docs/evidence/r08-installer.md`、`docs/evidence/r08-release.md` |
| T06 | 自动检查通过待实测：R08.1 已实现已捕获上下文的显式、有界预览；真实 page Provider、document/revision 变化、持久化和模型输入一致性仍待验收 | Codex | `72164511aaab3a3c2ad21fefb5bf595ef0d693cc` | `docs/evidence/r08-context-expansion.md`、`pnpm test:context-expansion` |
| T07 | 自动检查通过待实测：R08.2 已实现类型化界面文案、双主题、响应式、焦点和视觉基线；Narrator、DPI、多屏和真实窗口完整状态仍待验收 | Codex | `72164511aaab3a3c2ad21fefb5bf595ef0d693cc` | `docs/evidence/r08-ui.md`、`pnpm test:ui:a11y`、`pnpm test:ui:visual` |
| T08-A/B/C/D | 待开始，逐项选择 | 待分配 | — | — |
| T09 | 自动检查通过待实测：R08.3 已交付本地性能基准、匿名研究 fixture 校验和候选 SHA 绑定的描述性报告；真人研究和产品效果仍未验证 | Codex | `4aef646507b740110fbb8e8055e6f2a44d97f9aa` | `docs/evidence/r08-performance-human-factors.md`、`pnpm test:perf`、`pnpm test:human-factors:fixtures` |
| T10 | 自动检查通过待实测：R08.4 已交付版本/产物元数据校验和脱敏诊断探针；Tauri 安装包、干净环境安装升级卸载仍未运行 | Codex | 待生成安装候选 | `docs/evidence/r08-installer.md`、`node --test scripts/installer.test.mjs`、`pnpm test:installer` |
| T11 | 自动检查通过待实测：`check:release` 候选聚合器已实现；当前无 manifest 时返回 NOT READY/退出码 2，真实模型、宿主交互、安装、人因和发布证据仍待执行 | Codex | `c264c08ff41f4735553bb55da2015bc808bb7699` | `docs/evidence/r08-release.md`、`node --test scripts/release.test.mjs`、`pnpm check:release` |

### 9.2 PR 描述模板

```text
任务：Txx — 名称
问题与结果：具体用户触发、修改前后行为。
范围：修改的模块、文件所有者、未纳入本 PR 的任务。
接口：消息、事件、配置、持久数据或宿主版本是否变化。
用户可见行为：焦点、输入、材料范围、取消、恢复与本地化。
验证：工作目录、SHA、命令、退出码、通过/失败/跳过数量。
实际环境：Windows/浏览器/DPI/Harness/Node/pnpm/Rust。
人工证据：场景、结果、截图/录屏或记录位置。
未验证：明确原因、影响、验证者和所需条件。
决策记录：对应 Agent Note 或等价路径。
集成依赖：须先合并的接口或宿主变更。
发布影响：产物变化、已知限制和可恢复方式。
```

### 9.3 单项测试证据模板

```text
测试 ID：Txx-CASE-001
日期与执行者：
仓库、分支、提交 SHA：
是否存在未提交修改：
工作目录与工具链：
证据等级：S/U/I/W/L/H
前置状态与非敏感 fixture：
准确命令或手工步骤：
期望行为：
实际行为：
退出码与测试数量：
结果：PASS / FAIL / SKIP / NOT RUN
日志或截图位置：
失败原因或局限：
清理动作：
复核者：
```

### 9.4 Codex 任务结束交接模板

```text
已完成：本任务具体行为及交付物。
当前代码：仓库、分支、SHA 或未提交改动。
接口变化：下游需要关注的字段和调用方式。
实际检查：命令、退出码、数量和环境。
未验证/阻塞：缺什么、影响什么、由谁验证。
人工验收入口：启动方式、fixture、应看到的结果。
下一任务：编号、前置是否满足、可并行文件范围。
禁止误判：哪些结果只是 mock/构建，哪些才是实机或真人证据。
```

## 10. 最终完成定义

### 10.1 技术与产品候选

- [ ] 正确仓库与工具链可复现，候选 SHA、依赖和产物一致。
- [ ] 无扩展 Chrome/Edge 支持范围有真实 Windows 证据。
- [ ] 选区可清除、暂停、固定；来源和修订不串换。
- [ ] IPC 有超时、恢复、事件分发及明确访问策略。
- [ ] Lens 不被动抢焦点，键盘、中文输入法、多屏/DPI 可用。
- [ ] 解释、追问、停止和转入完整会话形成真实闭环；产品范围明确排除翻译入口。
- [ ] 实际模型输入可由宿主日志重建，重试不重复执行。
- [ ] 上下文范围可控、来源可核查、部分内容与截断可识别。
- [ ] 本地化、主题、可访问性、错误恢复与主要视觉状态完整。
- [ ] 安装、启动、诊断、退出、升级与卸载在隔离环境验证。
- [ ] 关键自动检查、Windows、真实模型和安装证据齐全，阻断缺陷为零。
- [ ] 可选 Provider 的支持声明与证据一致；未实现能力明确排除。

### 10.2 “感知增强”效果

- [ ] 真人测试具有同等难度任务、基线、顺序控制和可审查评分规则。
- [ ] 正确率、完成率、耗时、切换、误触和工作负荷分别报告。
- [ ] 性能指标对应可复现机器和样本，模型等待与 UI 延迟分开。
- [ ] 没有未经用户发起的模型请求，用户能识别正在处理的材料及来源。
- [ ] 已达到第 2/6 节的初始效果目标，或由用户审查具体数据后批准调整目标。
- [ ] 报告明确样本与平台局限，不把小样本或模拟研究推广为普遍结论。

只有 10.1 完成时，可以称为技术上可审查的产品候选；只有具备 10.2 的真实证据时，才能声称在被测场景中实现“感知增强”。发布操作仍按当次授权执行。

## 11. 依据与资料

### 11.1 项目源码依据

下列链接固定到审阅 SHA，便于后续确认计划的出发点。它们是基线证据，不替代开发时读取当前代码。

- [README：产品定位、已实现链路及待实现能力](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/README.md)
- [package.json：现有命令与依赖](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/package.json)
- [CaptureRuntime：触发、去重与发布](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/native/src-tauri/src/capture.rs)
- [BrowserAccessibilityProvider：文字、来源与几何](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/native/src-tauri/src/providers/browser_accessibility.rs)
- [Rust Bridge：连接、提交与请求交换](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/native/src-tauri/src/bridge.rs)
- [TS 路由：实际实现的能力](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/src/bridge/router.ts)
- [TS 协议：预留消息与验证](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/src/bridge/protocol.ts)
- [缓存：TTL、容量与修订](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/src/context/cache.ts)
- [App：现有诊断界面](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/native/src/App.tsx)
- [App 测试：当前模拟覆盖](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/native/src/App.test.tsx)
- [Tauri 配置：窗口与打包](https://github.com/mivaille777/Harness-plugins/blob/122005f75af1fd7ae0621b626de6fe0072139732/native/src-tauri/tauri.conf.json)

### 11.2 人机交互依据

- [Microsoft Research：Guidelines for Human-AI Interaction](https://www.microsoft.com/en-us/research/publication/guidelines-for-human-ai-interaction/)：用于能力说明、适时反馈、纠错、用户控制和长期交互原则；本文的具体界面方案是这些原则在该项目中的设计建议。
- [W3C：WCAG 2.2](https://www.w3.org/TR/WCAG22/)：用于 WebView 内容的可访问性要求；不能替代原生 Windows 的实际测试。
- [NASA：Task Load Index](https://www.nasa.gov/human-systems-integration-division/nasa-task-load-index-tlx/)：用于主观工作负荷测量；不能替代理解正确率和任务完成测试。

300ms/100ms、90%、25% 以及初步样本规模是本计划提出的产品和研究起点，不是这些来源给出的本项目性能承诺。

## 12. 文档维护说明

本文件是后续开发的任务规格，不是当前已发布产品文档。任务执行后更新第 9 节状态与证据，并将真实实现的配置、行为、限制和操作说明放回各自的 README/JSDoc/测试所有者处。不要把本计划整体复制进已实现 Agent Note。

新增命令在负责任务完成后可以从“拟新增”改为“已有”，同时记录定义位置和实际执行证据。接口或范围调整须说明影响的任务、fixture 和验收；不能只改提示词而留下相互冲突的验收要求。

截至 2026-09-09，工作线已交付 R01～R07 的协议、会话、材料绑定、历史恢复和分层运行器基础，并完成 R08.1 的有界上下文预览、R08.2 的感知优先 UI、locale 字典、响应式主题、无障碍检查和真实 Tauri WebView 截图、R08.3 的本地性能/人因研究基础设施、R08.4 的安装元数据/脱敏诊断基础以及 R08.5 的候选 manifest 校验器。R05 宿主交互、R07 L2/L3 真实模型与浏览器验收、R08.1 的真实 Provider/document/持久化验收、R08.3 真人效果、R08.4 安装升级卸载和 R08.5 发布候选仍未完成；详见 `docs/evidence/` 中的逐项状态。后续任务必须在当前提交上继续，不能把本地 fixture 或构建成功写成完整产品验收。
