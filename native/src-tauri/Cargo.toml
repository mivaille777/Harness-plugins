[package]
name = "dsh-selection-companion-native"
version = "0.1.0"
description = "Native Windows companion for dsh-selection-companion"
edition = "2021"
rust-version = "1.77.2"
publish = false
build = "build.rs"
default-run = "dsh-selection-companion-native"

[lib]
name = "dsh_selection_companion_native"
path = "src/lib.rs"

[[bin]]
name = "dsh-selection-companion-host"
path = "src/bin/native_host.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
tokio = { version = "1", features = ["io-util", "macros", "net", "rt-multi-thread", "sync", "time"] }

[target.'cfg(windows)'.dependencies]
uiautomation = "=0.16.1"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
